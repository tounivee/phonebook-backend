{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "Backend for Phonebook app",
  "license": "MIT",
  "author": "Tony Virtanen",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../FullStackOpen/part2/phonebook && npm run build && @powershell Copy-Item dist -Recurse ../../../phonebook-backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
